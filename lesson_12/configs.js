

export const SWIPER_CONFIG = {
  speed: 1000,
  pagination: {
    el: ".swiper-pagination",
    type: "fraction",
  },
  navigation: {
    nextEl: ".swiper-button-next",
    prevEl: ".swiper-button-prev",
  },
}


export const GLIGHTBOX_CONFIG = {
  type: 'image',
  selector: '.test',
  zoomable: true,
  draggable: true,
  width: '900px',
  height: 'auto',
  touchNavigation: true,
  loop: true,
  autoplayVideos: true,
  effect: 'fade'
}